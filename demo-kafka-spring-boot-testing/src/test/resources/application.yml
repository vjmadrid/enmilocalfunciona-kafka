spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    consumer:
      group-id: example-group
      auto-offset-reset: earliest
app:
  topic:
    example1: topic-1
    example2: topic-2

